{
  "permissions": {
    "allow": [
      "Bash(dotnet build*)",
      "Bash(dotnet test*)",
      "Bash(dotnet restore*)",
      "Bash(dotnet ef migrations list*)",
      "Bash(git -C * log*)",
      "Bash(git -C * status*)",
      "Bash(git -C * diff*)"
    ],
    "deny": [
      "Bash(dotnet ef database drop*)",
      "Bash(git push --force*)"
    ]
  }
}
